## ZJ Roadmap

> 数据文件: `2026-06-24-rust-hybrid-indexing-ownership-roadmap.json` | 最后更新: 2026-06-25 17:38:34


<!-- ROADMAP_TREE_START -->
<!-- 由 zj-roadmap-driven 自动生成，请勿手动编辑 -->
[~][X+] 1. Rust-Hybrid Indexing Completion And Performance Roadmap
├── [x][X+] 1-1. Current fact base and evidence archive
│   ├── [x][X+] 1-1-1. Validated facts from completed performance roadmaps
│   └── [x][X+] 1-1-2. Evidence documents retained as source material
├── [x][X+] 1-2. Rust-owned indexing completion boundary
│   ├── [x][X+] 1-2-1. Rust-owned supported language target set
│   └── [x][X+] 1-2-2. TypeScript shell and fallback ownership boundary
├── [x][Y+] 1-3. Resolver finalization ownership frontier
│   ├── [x][Y+] 1-3-1. Facts protocol first slice for LowerName QualifiedName FileNodes
│   ├── [x][X+] 1-3-2. Reference disambiguation semantic migration slices
│   └── [x][Y+] 1-3-3. Finalization cleanup and edge write ownership slices
│       ├── [x][Y+] 1-3-3-1. Guarded direct named import usage edge-write slice
│       └── [x][Y+] 1-3-3-2. Remaining cleanup and broader edge-write ownership slices
│           ├── [x][Y+] 1-3-3-2-1. Finalization cleanup ownership contract diagnostics
│           └── [x][Y+] 1-3-3-2-2. Broader edge-write ownership follow-up slices
│               ├── [x][Y+] 1-3-3-2-2-1. Module/file dependency guarded edge-write contract
│               └── [x][X+] 1-3-3-2-2-2. Future package/default/namespace/declaration-runtime edge-write ownership
│                   ├── [x][Y+] 1-3-3-2-2-2-1. Declaration/runtime edge-write ownership narrow slice
│                   └── [x][X+] 1-3-3-2-2-2-2. Future package/default/namespace edge-write ownership
│                       ├── [x][Y+] 1-3-3-2-2-2-2-1. Package self-name edge-write ownership slice
│                       └── [x][X+] 1-3-3-2-2-2-2-2. Future package imports default namespace edge-write ownership
│                           ├── [x][Y+] 1-3-3-2-2-2-2-2-1. Package imports # repo-local file-level edge-write ownership
│                           ├── [x][X+] 1-3-3-2-2-2-2-2-2. Default import edge-write ownership
│                           └── [x][Y+] 1-3-3-2-2-2-2-2-3. Namespace import edge-write ownership
├── [ ][X+] 1-4. Performance evidence lanes under ownership migration
│   ├── [ ][X+] 1-4-1. Baseline trend recording for every ownership slice
│   └── [ ][Y+] 1-4-2. Bounded performance exploit only when ownership progress needs it
├── [x][X+] 1-5. Agent Sufficiency and graph semantics guardrails
│   ├── [x][Y+] 1-5-1. Graph parity fallback taxonomy and RSS evidence
│   └── [x][Y+] 1-5-2. Agent Sufficiency trigger conditions
├── [~][X+] 1-6. No-go defer and research archive
│   ├── [x][X+] 1-6-1. Default no-go and diagnostic-only routes
│   └── [ ][X+] 1-6-2. Research and oracle-needed routes
└── [x][Y+] 1-7. Execution rules and first-stage closeout
    ├── [x][Y+] 1-7-1. First stage builds map only
    └── [x][Y+] 1-7-2. Next stage opens facts protocol implementation issues
<!-- ROADMAP_TREE_END -->

### 决策历史

| 节点 | 问题 | 答案 | 备注 |
|------|------|------|------|
| 1 | Root goal | Complete Rust-owned indexing/finalization roadmap reconstruction and make performance optimization serve that ownership migration mainline. | Chosen over continuing a narrow performance-candidate queue. |
| 1 | Tree trunk | Use ownership migration as the trunk; use end-to-end indexing path inside each ownership area; use user outcome as guardrails. | This avoids letting performance grow into a separate maze. |
| 1-2 | Ownership target | For JS, JSX, TS, TSX, MTS, CTS, and Go, indexing plus resolver plus finalization should become as Rust-owned as practical. | TypeScript remains product shell, fallback orchestration, non-Rust-owned language support, and explicitly unmigrated semantic guardrails. |
| 1-3 | First exploit frontier | Prioritize resolver/finalization ownership frontier. | VS Code sparse evidence points to finalization/reference-resolution tail after Rust core extraction/write and fallback append. |
| 1-4 | Performance role | Performance is a validation dimension under ownership migration, not an independent steering tree. | Each slice records trend evidence; bounded performance exploit opens only when it supports ownership progress or user usability. |
| 1-5 | 第11刀主目标选哪一块？ | A. Agent Sufficiency and graph semantics guardrails | Resolver/finalization ownership frontier 已完成；下一刀优先验证图语义和 agent sufficiency guardrails，避免后续性能优化在不可信语义上加速。 |
| 1-5 | 第11刀在 1-5 下先做哪个子节点？ | A. 先做 1-5-1 Graph parity fallback taxonomy and RSS evidence | 先钉住 graph parity、fallback taxonomy、RSS/unavailable reason 的证据口径，再决定 Agent Sufficiency 触发条件；避免 agent smoke 波动时无法归因。 |
| 1-6 | Archive policy | Keep no-go, defer, and research routes as explicit roadmap subtrees. | Prevents future agents from re-entering already tested failure modes. |
| 1-7 | First stage scope | Build the map only; do not split implementation issues in this stage. | The point of this stage is to return to the start and rebuild navigation from accumulated facts. |
| 1-3-1 | First slice layer | Start at the facts input layer by making Rust-produced facts a stable protocol boundary. | Do not start by changing final reference disambiguation semantics. |
| 1-3-1 | First facts protocol shapes | Cover LowerName, QualifiedName, and FileNodes with explicit keep, no-go, or diagnostic-only status. | Use prior evidence instead of redefining the full resolver input surface in one step. |
| 1-3-1 | First implementation result | Expose factsProtocol diagnostics for LowerName, QualifiedName, and FileNodes in rust-hybrid profile artifacts. | No resolver semantics or graph output changed; this is a protocol-status diagnostic slice. |
| 1-3-2 | Second slice target | Use shadow/double-read semantic replay before moving any reference disambiguation decision to the main path. | Rust/protocol may produce replay decisions for selected references, but TypeScript remains final owner of graph output in this slice. |
| 1-3-2 | Replay reference class | Start with direct ESM named import/export binding replay. | Cover import { foo } from repo-local relative or path-alias targets to exported foo; exclude default, namespace, type-only, barrel chains, package resolution, and fuzzy/name-matching ranking. |
| 1-3-2 | Replay evidence location | Record semantic replay evidence in profile artifact diagnostics. | Use a semanticReplay diagnostic bucket; do not write SQLite debug tables and do not create durable benchmark artifacts for every process run. |
| 1-3-2 | Replay mismatch taxonomy | Use a medium taxonomy for direct named import/export replay. | Include import-target-unresolved, export-symbol-missing, multiple-export-candidates, ts-unresolved-rust-resolved, ts-resolved-rust-unresolved, different-target-node, and different-resolution-method. Keep unsupported syntax families out of this slice. |
| 1-3-2 | Tracking issue | Track implementation in #521. | Issue covers semanticReplay profile diagnostics for direct named ESM import/export shadow replay. |
| 1-3-3 | Third slice main target | Guarded edge-write ownership slice | Prefer moving validated resolver/protocol decisions toward guarded edge-write ownership before cleanup ownership. Keep the slice narrow: no broad resolver semantic expansion, no package/default/namespace/re-export scope expansion by default. |
| 1-3-3 | Guarded edge-write target | Direct named import file/symbol edge write | Reuse the semanticReplay evidence area from #521. Start from direct named value imports with unique repo-local targets; defer LowerName and FileNodes/module-resolution edge-write routing to later slices. |
| 1-3-3 | Edge kind for guarded write | Only imported symbol usage edges | This slice should prove one per-reference semantic edge path for direct named imports. Do not include file-level binding dependency edges in this slice; keep those for module/file dependency ownership work. |
| 1-3-3 | Execution mode | Direct main-path write with fail-closed guards | Do not add another shadow-only middle state. Write only when direct named value import usage has a unique repo-local target and passes endpoint/duplicate guards; all unsupported or ambiguous cases skip with taxonomy. |
| 1-3-3 | Allowed graph output change | Allow guarded additive rust-finalization usage edges with target parity | Do not replace existing TypeScript-owned edges in this slice. Graph output may gain rust-finalization usage edges when target parity is preserved or when the edge was previously missing; duplicate-safe behavior is required. |
| 1-3-3 | Guarded edge-write skip taxonomy | Use medium-grained taxonomy | Track unsupported-import-shape, type-only-import, import-target-unresolved, export-symbol-missing, multiple-export-candidates, usage-ref-missing, endpoint-invalid, duplicate-equivalent-edge, and non-repo-local-target. Avoid ultra-fine moduleResolution taxonomy in this slice. |
| 1-3-3 | Profile diagnostics location | Add finalize.referenceResolutionBreakdown.guardedEdgeWrite | Keep guardedEdgeWrite beside semanticReplay. Include eligibleRefs, attemptedRefs, writtenEdges, skippedRefs, skipReasons, skipSamples, and edgeKindCounts so replay and guarded-write migration evidence are readable together. |
| 1-3-3 | Verification scope | Use deterministic CLI/profile and graph parity tests only | Cover written direct named value import usage edges, skipped unsupported forms, duplicate-safe behavior, guardedEdgeWrite profile fields, and targeted rust-hybrid CLI tests. Do not run Agent Sufficiency A/B or real-repo smoke for this narrow slice. |
| 1-3-3 | Slice output | Write a narrow plan and publish 2-3 implementation issues | The plan should preserve the grilled boundaries. Issues should cover guarded direct named import usage edge write/profile diagnostics, skip taxonomy and duplicate-safe parity coverage, and closeout roadmap/decision evidence if needed. |
| 1-3-3 | Closeout status for 1-3-3 | Do not mark 1-3-3 completed after this slice; create a child sub-node | This slice only covers guarded direct named import usage edge-write. Finalization cleanup and broader edge-write ownership remain under 1-3-3 for later slices. |
| 1-5-1 | 1-5-1 这刀的目标是证据契约收口还是跑新 baseline？ | A. 证据契约收口 | 第11刀不跑新真实 baseline，也不新造采样器；目标是定义/验证后续 slice closeout 必须回答 graphStats 稳定或变化可解释、fallback taxonomy 可见且可解释、RSS recorded 或 unavailable reason 明确。 |
| 1-5-1 | 1-5-1 的产物放在哪里？ | A. docs/benchmarks/graph-semantics-guardrail-v1.md baseline/guardrail 标准文件，并配轻量测试锁关键条款 | 这是后续 graph-semantic ownership closeout 的长期 evidence contract，不是一次性结果。docs/benchmarks 已承载 baseline-indexing-performance-v1，同类标准应放在这里。 |
| 1-5-1 | graph-semantics-guardrail-v1 的通过条件是否三件事缺一不可？ | A. 三件事缺一不可 | Graph-semantic/resolver/finalization ownership closeout 必须记录 graphStats stable 或变化可解释、fallback taxonomy 可见且可解释、RSS peakRssBytes 或明确 rssUnavailableReason。 |
| 1-5-1 | 第11刀是否直接做文档加轻量测试，不新建 implementation issues？ | A. 直接做文档加轻量测试 | 产物为 docs/benchmarks/graph-semantics-guardrail-v1.md 和 doc contract test；不新建 implementation issues，避免流程重于工作本身。 |
| 1-5-1 | 1-5-1 closeout 结果是什么？ | graph-semantics-guardrail-v1 completed | Added docs/benchmarks/graph-semantics-guardrail-v1.md and __tests__/graph-semantics-guardrail-doc.test.ts. Verification: npx vitest run __tests__/graph-semantics-guardrail-doc.test.ts. |
| 1-5-2 | 1-5-2 implementation issues 发布结果是什么？ | 已发布 #544 和 #545 | #544 定义 Agent Sufficiency trigger matrix；#545 锁定 doc contract test 与 roadmap closeout，blocked by #544。 |
| 1-5-2 | 1-5-2 closeout 结果是什么？ | 完成 #544 和 #545 | Agent Sufficiency trigger matrix 已写入 baseline-agent-sufficiency-v1；graph-semantics-guardrail-v1 只做 cross-reference，不复制策略；验证：npx vitest run __tests__/graph-semantics-guardrail-doc.test.ts。 |
| 1-3-3-1 | Published issues | Use #522, #523, and #524 | #522 implements guarded edge-write; #523 adds diagnostics and skip taxonomy coverage; #524 performs closeout and roadmap evidence updates. |
| 1-3-3-2 | Fourth slice main target | Finalization cleanup ownership slice | Prefer cleanup ownership before broader edge-write expansion so unresolved_refs deletion/retention semantics are explicit before more graph output is routed through Rust-owned paths. |
| 1-3-3-2 | Cleanup ownership execution mode | Contract and diagnostics first | Do not migrate cleanup into Rust core in this slice. First define and verify the TypeScript finalization cleanup contract: Rust core may clean refs it owns during Rust-owned resolution, while TypeScript finalization owns terminal cleanup for its resolved and intentionally-unresolved batch decisions. |
| 1-3-3-2 | Cleanup ownership verification boundary | Public CLI profile plus DB-observable contract tests | Use deterministic rust-hybrid fixtures through zcodegraph index --profile-out, then assert finalization cleanup profile fields and unresolved_refs terminal cleanup behavior. Do not require VS Code sparse smoke or Agent Sufficiency evidence for this narrow slice. |
| 1-3-3-2 | Cleanup ownership profile shape | Add narrow cleanupOwnership bucket | Expose finalize.referenceResolutionBreakdown.cleanupOwnership as contract diagnostics. It should summarize owner/mode, resolved terminal refs, intentionally-unresolved terminal refs, retained refs, rustCorePrecleanedRefs when reliably known or null otherwise, and notes. Do not expand into per-source taxonomy in this slice. |
| 1-3-3-2 | Fourth slice delivery shape | Write narrow plan document and publish 2-3 issues | Keep this as a contract-only finalization cleanup ownership slice. Publish small issues for cleanupOwnership diagnostics, deterministic CLI/DB contract tests, and closeout/roadmap evidence. |
| 1-3-3-2-2 | Fifth slice main target | Module/file dependency guarded edge-write contract | Use the existing Rust core moduleResolutionGuardedEdgeWrite profile and rust-finalization file-level imports edge path as the broader edge-write ownership entry point. Prefer contract diagnostics and deterministic graph tests before expanding default/namespace/package symbol semantics. |
| 1-3-3-2-2 | Module edge-write target scope | Relative and paths alias file-level imports only | Keep this as a contract slice over already-supported repo-local module targets. Do not expand rootDirs, package self-name, package imports, package exports, default imports, namespace imports, or symbol-level semantics in this fifth slice. |
| 1-3-3-2-2 | Module edge-write profile shape | Add finalize.referenceResolutionBreakdown.moduleEdgeWrite | Keep module/file-level imports edge-write separate from guardedEdgeWrite, which describes ESM named symbol/usage edges. moduleEdgeWrite should expose owner, mode, eligible/attempted/written/skipped counts, skipReasons, imports edgeKindCounts, supportedSources, and excludedSources. |
| 1-3-3-2-2 | Module edge-write graph verification | Relative and paths alias file-level imports edge graph tests | Verify file node to target file node imports edges with edgeOrigin rust-finalization, plus moduleEdgeWrite written count. Do not include symbol usage edges, default/namespace semantics, or VS Code sparse smoke in this slice. |
| 1-3-3-2-2 | Module edge-write skip coverage | Include one missing-target fail-closed skip test | Keep skip taxonomy bounded. Prefer a deterministic missing target file/import case to prove fail-closed behavior and moduleEdgeWrite.skipReasons without expanding into package/rootDirs/default/namespace resolver semantics. |
| 1-3-3-2-2 | Fifth slice delivery shape | Write narrow plan and publish three issues | Use three implementation issues: #528 moduleEdgeWrite profile diagnostics, #529 relative/paths-alias file-level graph tests, and #530 missing-target fail-closed skip coverage plus closeout evidence. |
| 1-3-3-2-2-2 | Sixth slice main target | Declaration/runtime edge-write ownership narrow slice | Continue from the completed module/file dependency guarded edge-write contract. Prefer high-confidence repo-local declaration-to-runtime pairing before expanding package/default/namespace edge-write semantics. |
| 1-3-3-2-2-2 | Declaration/runtime pairing scope | Repo-local .d.ts target to single runtime sibling only | Only rewrite guarded file-level imports edges from declaration targets to a unique runtime sibling. Multi-candidate, missing sibling, package/typeVersions/node_modules/symlink, and broader TypeScript moduleResolution semantics remain fail-closed taxonomy or future work. |
| 1-3-3-2-2-2 | Declaration/runtime diagnostics shape | Reuse moduleEdgeWrite.declarationRuntime | Expose declaration/runtime edge-write as a child bucket of the existing moduleEdgeWrite contract, not as a new top-level profile sibling. The behavior remains module/file dependency edge-write with a target rewrite sub-decision. |
| 1-3-3-2-2-2 | Declaration/runtime edge-write rollout mode | Direct main-path write for high-confidence single runtime sibling, fail-closed otherwise | Do not use shadow-only or local config for this narrow slice. Only rewrite file-level import edges when the declaration target has exactly one repo-local runtime sibling; all uncertain cases skip with taxonomy. |
| 1-3-3-2-2-2 | Declaration/runtime verification scope | Three deterministic CLI graph/profile tests | Cover happy path rewrite to unique runtime sibling, multi-candidate fail-closed skip, and missing-sibling fail-closed skip. Do not require VS Code sparse smoke, Agent Sufficiency A/B, full scoreboard, or packaged release smoke. |
| 1-3-3-2-2-2 | Sixth slice issue breakdown | Three issues: diagnostics, happy-path write, fail-closed closeout | Keep the sixth slice independently grabbable: one issue for moduleEdgeWrite.declarationRuntime diagnostics, one for unique runtime sibling main-path edge write, and one for multi-candidate/missing-sibling fail-closed taxonomy plus roadmap/plan closeout. |
| 1-3-3-2-2-2-2 | Seventh slice main target | Package self-name / package exports repo-local file-level edge-write | Continue the module/file dependency ownership line after relative/paths alias and declaration/runtime slices. Prefer repo-local package self-name and package exports file targets before package imports or default/namespace binding-level semantics. |
| 1-3-3-2-2-2-2 | Package self-name edge-write scope | Repo-local package self-name root plus direct subpath/export target only | Cover package name imports that resolve to repo-local file targets. Keep package imports (#foo), external/node_modules packages, default/namespace binding semantics, and broad package exports/imports semantics out of this slice. |
| 1-3-3-2-2-2-2 | Package exports condition object scope | Include Rust-core-resolved single repo-local file targets only | Allow package exports condition objects when existing Rust core resolution produces one repo-local file target. Reuse declaration/runtime rewrite when the resolved target is a declaration file. Do not broaden into full TypeScript or Node package exports parity. |
| 1-3-3-2-2-2-2 | Package self-name diagnostics shape | Reuse moduleEdgeWrite.packageSelfName | Expose package self-name edge-write as a child bucket of moduleEdgeWrite, not as a new top-level profile sibling. Existing raw importPathAliasPackageSelfName outcome fields remain resolution diagnostics, while this bucket describes edge-write ownership. |
| 1-3-3-2-2-2-2 | Package self-name rollout mode | Direct main-path write for Rust-resolved repo-local file targets, fail-closed otherwise | Do not use shadow-only or local config for this narrow slice. If existing Rust core package self-name/package exports resolution produces a repo-local file target, guarded edge-write may write the rust-finalization imports edge; uncertain outcomes remain skip taxonomy. |
| 1-3-3-2-2-2-2 | Package self-name verification scope | Three deterministic CLI graph/profile tests | Cover package self-name root happy path, package exports condition object with declaration/runtime reuse, and one fail-closed unsupported/escaping/missing target taxonomy case. Do not require VS Code sparse smoke, Agent Sufficiency A/B, full scoreboard, or release smoke. |
| 1-3-3-2-2-2-2 | Seventh slice issue breakdown | Three issues: packageSelfName diagnostics, happy-path edge-write, exports/fail-closed closeout | Keep the slice independently grabbable: one issue for moduleEdgeWrite.packageSelfName diagnostics, one for package self-name root/direct target edge-write coverage, and one for package exports condition object plus fail-closed taxonomy and roadmap/plan closeout. |
| 1-3-3-2-2-2-2-2 | 第八刀主目标选哪一块？ | A. package imports #foo repo-local file-level edge-write ownership | 保持在 module/file dependency edge-write ownership 主线；default/namespace 属于 binding-level symbol semantics，后续单独拆。 |
| 1-3-3-2-2-2-2-2 | package imports #foo 的解析范围做到哪里？ | A. nearest package boundary imports map only，direct key / pattern / condition object，repo-local file target | 这刀只接 Rust core 已支持并已解析出的 package imports repo-local file target；不新增 external package resolution，不混入 default/namespace binding semantics。 |
| 1-3-3-2-2-2-2-2 | package imports edge-write diagnostics 如何暴露？ | A. 在 moduleEdgeWrite 下新增 packageImports 子桶，对齐 packageSelfName 字段 | 保持 module/file dependency edge-write diagnostics 的单一入口；不新开 top-level bucket。 |
| 1-3-3-2-2-2-2-2 | package imports edge-write rollout 怎么做？ | A. 直接 main-path guarded write，fail-closed skip | Rust core 已解析到 repo-local file target 时写 imports edge；无法高置信解析时不回退同语言 TS fallback，只记录 skip taxonomy。 |
| 1-3-3-2-2-2-2-2 | package imports 解析到 .d.ts target 时怎么处理？ | A. 复用 declaration/runtime rewrite，唯一 runtime sibling 才写 | package imports 不另造 declaration/runtime 语义；唯一 runtime sibling 才写到 runtime file，失败计入 fail-closed taxonomy。 |
| 1-3-3-2-2-2-2-2 | 第八刀验证门槛是什么？ | A. deterministic CLI graph/profile tests only | 覆盖 direct key happy path、pattern/condition object/.d.ts runtime rewrite、fail-closed taxonomy；不跑 VS Code sparse、Agent A/B 或完整 scoreboard。 |
| 1-3-3-2-2-2-2-2 | 宽父节点如何避免 package imports/default/namespace 误判完成？ | A. 拆 3 个 sub-node，package imports 本刀完成，其余 pending | 保留父节点原语义；本刀只完成 package imports # repo-local file-level edge-write ownership，default import 和 namespace import 继续作为独立待办。 |
| 1-3-3-2-2-2-2-2-1 | 第八刀拆成几个 implementation issues？ | A. 3 issues：diagnostics / happy-path edge-write / fail-closed+closeout | 顺序为 moduleEdgeWrite.packageImports diagnostics contract、package imports direct/pattern/condition repo-local edge-write、fail-closed taxonomy + declaration/runtime reuse closeout。 |
| 1-3-3-2-2-2-2-2-1 | 第八刀 implementation issues 发布了吗？ | 已发布 #537, #538, #539 | #537 diagnostics；#538 package imports repo-local file-level edge-write；#539 fail-closed taxonomy + declaration/runtime closeout。 |
| 1-3-3-2-2-2-2-2-2 | 第九刀主目标选哪一块？ | A. default import edge-write ownership | 先完成 default binding ownership；namespace import 语义更偏 whole-module namespace/member-access backstop，留到后续单独处理。 |
| 1-3-3-2-2-2-2-2-2 | default import edge-write 的目标语义是什么？ | A. binding-level symbol edge，限 repo-local direct default import/export | 保留既有 file-level imports edge；本刀完成 default import binding 到 default exported symbol/export node 的 guarded edge-write，不做 namespace、CJS、export=、re-export chain 或 external package default import。 |
| 1-3-3-2-2-2-2-2-2 | default import 覆盖哪些 export shape？ | A. 只覆盖 direct default function/class declaration；其他 default 形态 taxonomy skip | 利用现有 Rust direct default declaration path。export default x、one-hop default re-export chain、CJS/default interop 都不进第九刀。 |
| 1-3-3-2-2-2-2-2-2 | default import edge-write diagnostics 放哪里？ | A. 复用 guardedEdgeWrite，保留和验证 default-specific skip reasons and samples | default import 是 binding-level symbol edge-write，归属 guardedEdgeWrite；不新增 defaultImportEdgeWrite top-level，也不新增独立 moduleEdgeWrite 子桶。 |
| 1-3-3-2-2-2-2-2-2 | default import edge-write rollout 怎么做？ | A. main-path guarded write contract，限 direct default declaration | 锁定已有高置信 Rust主路径；candidate 0/multiple 或非 direct default declaration 进入 guardedEdgeWrite taxonomy，不加 shadow/config/env 中间态。 |
| 1-3-3-2-2-2-2-2-2 | 第九刀验证门槛是什么？ | A. deterministic CLI graph/profile tests only | 覆盖 direct default function/class happy path、candidate-zero fail-closed taxonomy、scope guard；不跑 VS Code sparse、Agent A/B 或 full scoreboard。 |
| 1-3-3-2-2-2-2-2-2 | 第九刀拆成几个 implementation issues？ | A. 2 issues：happy-path contract / fail-closed closeout | 主路径已有 Rust core 基础；第一个 issue 锁 direct default function/class graph/profile contract，第二个 issue 锁 candidate taxonomy、scope exclusions 和 roadmap closeout。 |
| 1-3-3-2-2-2-2-2-2 | 第九刀 implementation issues 发布了吗？ | 已发布 #540, #541 | #540 default import direct declaration guarded edge-write contract；#541 default import fail-closed taxonomy and scope closeout。 |
| 1-3-3-2-2-2-2-2-3 | 第10刀主目标是否锁定 namespace import edge-write ownership？ | A. 锁定 1-3-3-2-2-2-2-2-3 Namespace import edge-write ownership | Package imports # 和 default import 已完成；namespace 是该父节点下唯一未完成叶子，优先收口它。 |
| 1-3-3-2-2-2-2-2-3 | Namespace import edge-write ownership 的语义是否展开到 NS.member 精确符号解析？ | A. 只做 whole-module/file-level dependency backstop，不做 NS.member -> exported symbol 逐成员解析 | 现有 TS resolver 也是 namespace import -> module file dependency backstop；逐成员解析属于更大的 resolver semantic slice，不能混入第10刀。 |
| 1-3-3-2-2-2-2-2-3 | 第10刀 namespace import edge-write ownership 是否限定 repo-local file-level contract？ | A. 限定 repo-local namespace import file-level edge-write contract | 覆盖 import * as NS from './mod' 以及当前 file-level resolver 已支持的 repo-local target；写 importing file -> module file 的 imports edge。外部包 namespace、NS.member 符号边不纳入本刀。 |
| 1-3-3-2-2-2-2-2-3 | Namespace import edge-write ownership 的公开诊断如何表达？ | A. 从 moduleEdgeWrite.excludedSources 移除 namespaceImports，并复用现有 moduleEdgeWrite 总桶 | Namespace import 本刀语义是 whole-module file-level edge-write，与现有 moduleEdgeWrite 同类；不新增长期子桶，避免 profile API 过度扩张。 |
| 1-3-3-2-2-2-2-2-3 | 第10刀验证门槛是否限定 deterministic CLI graph/profile tests？ | A. 只要求 deterministic CLI graph/profile tests | 覆盖 namespace import happy-path file->file imports edge、moduleEdgeWrite profile attempted/written、excludedSources 移除 namespaceImports、missing target fail-closed taxonomy；不跑 VS Code sparse、Agent A/B 或完整 scoreboard。 |
| 1-3-3-2-2-2-2-2-3 | 第10刀 namespace import edge-write ownership 是否拆成 2 个 issue？ | A. 拆成 2 个 issue | Issue 1 覆盖 happy-path namespace import file-level edge-write 与 excludedSources 更新；Issue 2 覆盖 fail-closed taxonomy、profile contract 与 roadmap closeout。 |
| 1-3-3-2-2-2-2-2-3 | 第10刀 GitHub issue 发布结果是什么？ | 已发布 #542 和 #543 | #542 覆盖 namespace import happy-path file-level edge-write 与 excludedSources 更新；#543 覆盖 fail-closed taxonomy、profile contract 与 roadmap closeout，blocked by #542。 |
| 1-3-3-2-2-2-2-2-3 | 第10刀 implementation closeout 结果是什么？ | #542/#543 completed | Namespace import whole-module file-level dependency ownership is covered by deterministic CLI graph/profile tests. Public moduleEdgeWrite no longer excludes namespaceImports; fail-closed unresolved namespace imports report target-not-found without unsafe edge writes. Verification: npx vitest run __tests__/rust-index-engine-cli.test.ts. |
